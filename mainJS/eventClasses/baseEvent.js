class baseEvent {

    // tick is the placement tick
    // type is the event type
    
    constructor (tick, type) {
        this.tick = tick;
        this.type = type;
    }
}

module.exports = {baseEvent}
